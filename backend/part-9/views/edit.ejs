<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white">

    <div class="min-h-screen flex items-center justify-center">
        <form 
            action="/edit/<%= user._id %>" 
            method="post"
            class="w-full max-w-md bg-zinc-800 p-8 rounded-2xl shadow-xl space-y-5"
        >

            <h2 class="text-2xl font-bold text-center">
                Edit Profile
            </h2>

            <!-- Username -->
            <div>
                <label class="block text-sm text-zinc-400 mb-1">
                    Username
                </label>
                <input 
                    type="text"
                    name="username"
                    value="<%= user.username %>"
                    class="w-full px-4 py-3 rounded-lg bg-zinc-900 border border-zinc-700 outline-none focus:ring-2 focus:ring-blue-500/40"
                    required
                />
            </div>

            <!-- Email -->
            <div>
                <label class="block text-sm text-zinc-400 mb-1">
                    Email
                </label>
                <input 
                    type="email"
                    name="email"
                    value="<%= user.email %>"
                    class="w-full px-4 py-3 rounded-lg bg-zinc-900 border border-zinc-700 outline-none focus:ring-2 focus:ring-blue-500/40"
                    required
                />
            </div>

            <!-- Age -->
            <div>
                <label class="block text-sm text-zinc-400 mb-1">
                    Age
                </label>
                <input 
                    type="number"
                    name="age"
                    value="<%= user.age %>"
                    class="w-full px-4 py-3 rounded-lg bg-zinc-900 border border-zinc-700 outline-none focus:ring-2 focus:ring-blue-500/40"
                />
            </div>

            <!-- Submit Button -->
            <button 
                type="submit"
                class="w-full py-3 bg-blue-600 rounded-lg font-semibold hover:bg-blue-700 transition"
            >
                Update Profile
            </button>

            <!-- Back to Dashboard -->
            <a 
                href="/dashboard"
                class="block text-center text-sm text-zinc-400 hover:text-white transition"
            >
                ← Back to Dashboard
            </a>

        </form>
    </div>

</body>
</html>
