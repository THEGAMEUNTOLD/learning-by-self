<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: "#3b82f6",
                        danger: "#ef4444",
                        card: "#27272a",
                        surface: "#18181b"
                    }
                }
            }
        }
    </script>
</head>

<body class="bg-gradient-to-br from-black via-zinc-900 to-black flex items-center justify-center min-h-screen text-white px-4">

    <form action="/edit/<%= user._id %>" method="post" class="bg-card w-full max-w-md p-8 rounded-2xl shadow-lg space-y-6">

        <h1 class="text-2xl font-bold text-center">Edit Profile</h1>

        <input type="text" name="username" value="<%= user.username %>" placeholder="Username"
            class="w-full px-4 py-3 rounded-lg bg-surface border border-zinc-700 text-white focus:border-primary focus:ring-2 focus:ring-primary outline-none" />
        <input type="email" name="email" value="<%= user.email %>" placeholder="Email"
            class="w-full px-4 py-3 rounded-lg bg-surface border border-zinc-700 text-white focus:border-primary focus:ring-2 focus:ring-primary outline-none" />
        <input type="number" name="age" value="<%= user.age %>" placeholder="Age"
            class="w-full px-4 py-3 rounded-lg bg-surface border border-zinc-700 text-white focus:border-primary focus:ring-2 focus:ring-primary outline-none" />

        <button type="submit" class="w-full py-3 rounded-lg bg-primary hover:bg-blue-500 font-semibold transition">
            Save Changes
        </button>

        <a href="/dashboard" class="block text-center text-sm text-zinc-400 hover:text-white transition">
            ← Back to Dashboard
        </a>

    </form>

</body>
</html>
