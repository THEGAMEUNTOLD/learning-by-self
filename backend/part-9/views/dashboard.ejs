<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white">

    <div class="min-h-screen flex items-center justify-center px-4">
        <div class="w-full max-w-lg bg-zinc-800 rounded-2xl shadow-xl p-8 space-y-6">

            <!-- Header -->
            <div class="text-center">
                <h1 class="text-3xl font-bold">Dashboard</h1>
                <p class="text-zinc-400 mt-1">
                    Welcome, <span class="text-white font-semibold"><%= user.username %></span> 👋
                </p>
            </div>

            <!-- User Info -->
            <div class="space-y-3 text-sm">
                <div class="flex justify-between bg-zinc-900 p-3 rounded-lg">
                    <span class="text-zinc-400">User ID</span>
                    <span class="font-medium break-all"><%= user._id %></span>
                </div>

                <div class="flex justify-between bg-zinc-900 p-3 rounded-lg">
                    <span class="text-zinc-400">Username</span>
                    <span class="font-medium"><%= user.username %></span>
                </div>

                <div class="flex justify-between bg-zinc-900 p-3 rounded-lg">
                    <span class="text-zinc-400">Email</span>
                    <span class="font-medium"><%= user.email %></span>
                </div>

                <div class="flex justify-between bg-zinc-900 p-3 rounded-lg">
                    <span class="text-zinc-400">Age</span>
                    <span class="font-medium"><%= user.age %></span>
                </div>
            </div>

            <!-- Actions -->
            <div class="space-y-3">
                <form action="/logout" method="post">
                    <button
                        type="submit"
                        class="w-full py-3 bg-red-600 rounded-lg font-semibold hover:bg-red-700 transition active:scale-95"
                    >
                        Logout
                    </button>
                </form>

                <a
                    href="/"
                    class="block text-center text-sm text-blue-400 hover:underline"
                >
                    Go to Home
                </a>
            </div>

        </div>
    </div>

</body>
</html>
