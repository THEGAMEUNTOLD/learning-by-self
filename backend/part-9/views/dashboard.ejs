<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: "#3b82f6",
                        danger: "#ef4444",
                        card: "#27272a"
                    }
                }
            }
        }
    </script>
</head>

<body class="bg-gradient-to-br from-zinc-900 via-black to-zinc-900 flex items-center justify-center min-h-screen text-white px-4">

    <div class="bg-card w-full max-w-lg p-8 rounded-2xl shadow-lg space-y-6">

        <div class="text-center">
            <h1 class="text-3xl font-bold">Dashboard</h1>
            <p class="text-zinc-400 mt-1">
                Welcome, <span class="text-white font-semibold"><%= user.username %></span> 👋
            </p>
        </div>

        <div class="space-y-3 text-sm">
            <div class="flex justify-between bg-surface/70 p-3 rounded-xl">
                <span class="text-zinc-400">Email</span>
                <span><%= user.email %></span>
            </div>
            <div class="flex justify-between bg-surface/70 p-3 rounded-xl">
                <span class="text-zinc-400">Age</span>
                <span><%= user.age %></span>
            </div>
        </div>

        <a href="/edit"
            class="w-full block py-3 rounded-lg bg-primary hover:bg-blue-500 text-center font-semibold transition">
            Edit Profile
        </a>

        <form action="/logout" method="post">
            <button type="submit"
                class="w-full py-3 rounded-lg bg-danger hover:bg-red-600 font-semibold transition">
                Logout
            </button>
        </form>

    </div>

</body>
</html>
